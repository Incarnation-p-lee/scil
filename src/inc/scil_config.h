#ifndef HAVE_DEFINED_SCIL_CONFIG_H
#define HAVE_DEFINED_SCIL_CONFIG_H

typedef enum grammar_type e_gr_type_t;

enum grammar_type {
    GR_TYPE_UNKNOWN,
    GR_TYPE_LL_1,
    GR_TYPE_LR_1,
};

#endif

